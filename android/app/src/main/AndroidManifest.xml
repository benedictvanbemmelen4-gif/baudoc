<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Wetterabruf im Bautagebuch (Open-Meteo) und kuenftige Server-Synchronisierung.
         Stand bisher nur im Debug-Manifest, sodass im Release-Build jeder
         Netzaufruf stumm scheiterte. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Standort. Der ungefaehre genuegt fuer das Wetter im Bautagebuch
         (lib/weather.dart, LocationAccuracy.low); Geofencing verlangt
         zwingend den genauen. geolocator bringt beides nicht selbst mit. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <!-- Ankunftserkennung bei geschlossener App. Muss vom Nutzer GETRENNT und
         NACH der Standortfreigabe erteilt werden (Android 11+ lehnt eine
         kombinierte Abfrage stillschweigend ab), siehe
         AndroidGeofenceGateway.requestPermission.
         Achtung Play-Veroeffentlichung: diese Berechtigung ist
         genehmigungspflichtig und verlangt ein Formular samt Video. Im
         internen Testkanal entfaellt das. -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>

    <!-- Ab Android 13 muss der Nutzer Benachrichtigungen erlauben; ohne sie
         kaeme weder die Ankunftsfrage noch die Pausenerinnerung an. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Geplante Alarme (Pausenende, Vorwarnung, Kappung) ueberstehen einen
         Geraeteneustart nur mit diesem Recht; Androids Geofences muessen nach
         dem Booten ohnehin neu gesetzt werden. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- Punktgenaue Alarme. Ohne sie verschiebt das System die
         Pausenerinnerung um bis zu eine Viertelstunde. USE_EXACT_ALARM gilt
         ab Android 14 ohne Nachfrage, SCHEDULE_EXACT_ALARM davor. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>

    <application
        android:label="BauDoc"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
